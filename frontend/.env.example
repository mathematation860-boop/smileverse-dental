# Copy this file to .env and fill in real values.

# URL of the deployed backend (see backend/.env.example).
REACT_APP_API_URL=http://localhost:5000

# Which practice this build talks to (see backend/config/practices/).
# Only relevant once a second practice exists.
REACT_APP_PRACTICE_ID=smileverse-dental
